{
  "name": "mmrunners-membership",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build":  "rm -f dist/*.html && tsc && cp frontend/*.html dist/ && cp frontend/i18n.js dist/ && cp appsscript.json dist/",
    "build:copy": "rm -f dist/*.html && tsc && cp frontend/*.html dist/ && cp frontend/i18n.js dist/ && cp appsscript.json dist/",
    "push": "clasp push",
    "deploy": "clasp deploy",
    "build:push": "npm run build:copy && npm run push",
    "test": "jest",
    "test:watch": "jest --watch",
    "otp:cleanup": "clasp run cleanupExpiredOtps"
  },
  "devDependencies": {
    "@google/clasp": "^3.2.0",
    "@types/google-apps-script": "^1.0.100",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.4",
    "typescript": "^5.4.0"
  }
}
