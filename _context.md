### 07-28 UTC — AZ1 (Azure/Stripe go-live): TEST-payment filters, V036, schema snapshot
Changed: payment_helpers.exclude_test_payments() + wired into api_sync_membership (membership-fee sync) and api_audit_members (both drift/never-matched SQL) so 'Stripe (TEST)' rows can't stamp real member fields or hide gaps; MIGRATION_V036 redefines sp_reconcile_member_payments + sp_renewal_audit with the same filter (bodies verbatim from SHOW CREATE, V035 was latest applied); fixed api_schema._tsv (no \n/\t/\\ escaping → regenerated snapshots diffed as ~1300 bogus lines) and regenerated db/schema_snapshot.sql (+11/-3 = exactly V033/V034/V035: stripe_events+livemode, submissions.MemberID nullable, trg_submissions_insert_validate). Status: pytest 1435 pass + 14 new (test_test_mode_payment_filter, test_schema_export_format), SQL ENUM lint clean, import-parity failures (3 sync_config modules) pre-existing on main. Prod still sk_test_ + STRIPE_ALLOW_TEST_FULFILLMENT=1, /api/payments/stripe/mode → testMode:true. Committed 114b39b → PR #23 (merged origin/main in; no V036 collision on main). Also delivered a 1-page CFO docx brief (scratchpad, not in repo).
Next: user does live-key swap (code needs NO change — the gate already defaults to ignoring test events); re-run /api/export-schema after V036 deploys (it changes 2 routine bodies); prod test row = anonymous $1 donation SUB-20260720-1USF2 — note sp_cancel_payment CANNOT remove it (SIGNALs on NULL MemberID), left in place, now filtered from reports.
### 07-28 UTC — P1j: finish withApiHandler adoption (auth throws → 401/403, not 500)
Changed: `requireSession()` now throws a status-carrying error via new `lib/http-error.ts` `httpError()` (it had none → wrapper mapped it to 500); `requireActiveMember` reuses it; new `lib/auth/admin.ts` `requireAdmin()` (401/403) replaces the session+isAdmin+403 boilerplate duplicated in all 10 `app/api/nyrr/**` routes, which now use `withApiHandler` instead of hand-rolled `catch (err: any)` + `err.message === 'Unauthorized'` string-sniffing; wrapped the 2 stragglers (`members/search` — was fully unwrapped; `admin/sync-status` — mapped auth throws to 500); the 3 txn routes (match, match/[id], edit-name) now acquire the pool connection AFTER auth+validation and release in `finally` — they previously **leaked a connection on every successful match** (release only ran in error branches). Status: 51 suites / 515 tests PASS incl. new `__tests__/lib/auth-admin.test.ts`, coverage floors hold, `next lint` clean, tsc clean except 2 pre-existing `Cannot find module 'stripe'` (package.json has ^22.3.2 but it's missing from the local node_modules — `npm install` needed; blocks `npm run build` locally, CI's npm ci is fine). -129 net LOC. Test churn: 12 files' auth-rejection mocks now carry `status: 401` (mirrors the real guard); 3 txn tests assert "no connection acquired" instead of release-on-early-return. Committed on claude/dev-plan-next-steps-3a064f. Next: `app/api/members/me` still maps **any** GET error (incl. DB failures) to 401 via a bare `catch` — worth wrapping, but it changes a documented contract; then P1j Flask payments tests.

### 07-21 UTC — P2 code-health: split lib/email/templates.ts (505→28 barrel)
Changed: templates.ts 505 LOC → 28-line barrel re-exporting 9 *EmailHtml() fns; extracted _layout.ts (wrap+header+portalFooter+APP_URL/PORTAL/FEEDBACK 68), templates/membership.ts (welcome/activated/renewal/expirationRepaired 174), templates/payments.ts (applicationReceived/rejected/expired/autoMatch 233), templates/auth.ts (passwordReset 53). Pure refactor, verbatim moves. Status: all ≤233 (<300); tsc clean (only pre-existing stripe not-installed errors); jest __tests__/lib/email-templates.test.ts 25/25 PASS via symlinked node_modules. Barrel keeps caller imports unchanged (1 route + 2 test files). Committed on claude/split-email-templates. Next P2 offenders: faq/page.tsx 471, lib/db/photos.ts 456.

