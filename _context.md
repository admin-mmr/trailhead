### 07-29 UTC — V036 deployed + post-deploy snapshot; migrations do NOT auto-run
Changed: PR #23 merged (9e1460c); V036 applied via `gh workflow run run-db-migrations.yml -f migration_version=V036` and VERIFIED against prod (schema_migrations→V036, both sp_reconcile_member_payments + sp_renewal_audit bodies contain the 'Stripe (TEST)' filter, dry-run CALL clean → 8 pre-existing drift rows, nothing written); workflow archived the file to db/archive/ and pushed 0e065b5 [skip ci]; regenerated db/schema_snapshot.sql (+2/-2 = exactly the two routine bodies — the api_schema._tsv escaping fix is what makes this diff readable). Status: CORRECTED a stale CLAUDE.md claim I had relied on — migrations do NOT auto-run on push; run-db-migrations.yml has its push trigger commented out and is workflow_dispatch-only, so merging a PR ships the code but NOT the SQL. CLAUDE.md Database section rewritten accordingly.
Next: AZ1 remainder is all CFO-side (Stripe account activation, live-key swap, then delete STRIPE_ALLOW_TEST_FULFILLMENT); PR #22's context entry claims Azure STRIPE_WEBHOOK_SECRET is a 7-char placeholder + NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY unset — contradicts my 07-28 read (full whsec_, testMode:true, unsigned webhook→400); re-verify before go-live.

### 07-28 UTC — AZ1 (Azure/Stripe go-live): TEST-payment filters, V036, schema snapshot
Changed: payment_helpers.exclude_test_payments() + wired into api_sync_membership (membership-fee sync) and api_audit_members (both drift/never-matched SQL) so 'Stripe (TEST)' rows can't stamp real member fields or hide gaps; MIGRATION_V036 redefines sp_reconcile_member_payments + sp_renewal_audit with the same filter (bodies verbatim from SHOW CREATE, V035 was latest applied); fixed api_schema._tsv (no \n/\t/\\ escaping → regenerated snapshots diffed as ~1300 bogus lines) and regenerated db/schema_snapshot.sql (+11/-3 = exactly V033/V034/V035: stripe_events+livemode, submissions.MemberID nullable, trg_submissions_insert_validate). Status: pytest 1435 pass + 14 new (test_test_mode_payment_filter, test_schema_export_format), SQL ENUM lint clean, import-parity failures (3 sync_config modules) pre-existing on main. Prod still sk_test_ + STRIPE_ALLOW_TEST_FULFILLMENT=1, /api/payments/stripe/mode → testMode:true. Committed 114b39b → PR #23 (merged origin/main in; no V036 collision on main). Also delivered a 1-page CFO docx brief (scratchpad, not in repo).
Next: user does live-key swap (code needs NO change — the gate already defaults to ignoring test events); re-run /api/export-schema after V036 deploys (it changes 2 routine bodies); prod test row = anonymous $1 donation SUB-20260720-1USF2 — note sp_cancel_payment CANNOT remove it (SIGNALs on NULL MemberID), left in place, now filtered from reports.
### 07-28 UTC — Stripe join demo: fixed 3 bugs that made new-member enrollment impossible
Changed: `lib/db/members.ts createNewMember()` — (a) `CALL generate_member_id(@next_id); SELECT @next_id` was one `conn.execute()`, which mysql2 rejects (prepared stmts never allow multi-statement; pool has no `multipleStatements`) → **every** new-member insert 500'd with ER_PARSE_ERROR; split into two `conn.query()` calls on the same pooled conn. (b) It then wrapped the proc's output as `MMR-${year}-${padStart(next_id)}` but the proc already returns canonical `A0667`; since `generate_member_id` derives max via `SUBSTRING(MemberID,2)`, an `MMR-` row would CAST to 0 and collide every later member at A0001 — now uses `next_id` verbatim. Proof it never worked: all 655 members are `A####`, zero `MMR-%`. (c) `api/payments/submit` required `phone` (min 7) while InfoStep labels it "(optional)" and `members/enroll` allows undefined → members who skipped it got a MemberID then a bare "Invalid input" at the Pay step; aligned to enroll's preprocess+optional. Also fixed Azure SWA `STRIPE_SECRET_KEY`, which held a fake 9-char `sk_live_?` placeholder — `isStripeTestMode()` read it as live, so the amber "no real charge" banner was suppressed on prod while no charge was actually possible; set to the real `sk_test_` key, `/api/payments/stripe/mode` now returns `{"testMode":true}`. Status: demoed /join → Checkout end-to-end locally (test fulfillment on, `stripe listen`); A0667 created → paid $30 → webhook `checkout.session.completed` processed → gmail_transactions + payments (`Stripe (TEST)`, memo `TEST — …`) → submission approved → member active/2027-03-31. 51 suites / 515 tests PASS, tsc clean. Demo rows deleted afterward (highest MemberID back to A0666). Next: no test covers `createNewMember` — add one asserting the `A####` shape and the two-step ID call, which is how this shipped broken; Azure `STRIPE_WEBHOOK_SECRET` is still a 7-char placeholder and `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` is unset, so prod card payments remain non-functional until a real dashboard webhook endpoint exists.

