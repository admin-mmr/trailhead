### 07-29 UTC — P1L Session 3: event RSVP + shared roster with privacy opt-out
Changed: `POST/DELETE /api/events/[id]/rsvp` (upsert via the V037 unique key — values passed twice as params since `VALUES()` in the UPDATE clause is deprecated in MySQL 8.0.20+; 404 on unknown event checked *before* the write so the FK can't become a 500; **409 on a past race**, race day itself still open) + `GET /api/events/[id]/roster`. **The `ShowRsvpPublicly` filter lives in `getEventRoster()`, not the route** — opted-out members are counted via `hiddenCount` but neither their name nor their note enters the payload, and no future caller can leak one by forgetting to filter. `not_going` counted, never listed; nameless members fall back to MemberID, never email. Opt-out plumbed `Member.showRsvpPublicly` → `rowToMember` (absent/NULL ⇒ **true**, matching the DB default) → `updateMemberProfile` (writes 1/0) → `PATCH /api/members/me` (`z.boolean()`, so `"false"`/`0` are rejected not coerced) → checkbox on `/portal/profile`. New `_components/{RsvpControls,RosterPanel,EventCard}`; re-tapping the selected intent clears it. Also moved month nav out of `MonthGrid` into the container — the grid is hidden below `lg`, so nav inside it was unreachable on mobile — and the card list now renders in month view too, since a desktop member on the grid otherwise had no way to RSVP.
Status: jest **693 pass / 0 fail** across 62 suites (+55: 26 rsvp route, 14 roster incl. asserting the opted-out name/id/note appear nowhere in the serialized body, 15 RSVP/roster UI incl. the 307-returns-HTML case, plus privacy-column mapping and PATCH boolean-strictness). tsc + eslint clean, `npm run build` green, coverage 75%/87%/77% vs floors 30/75/43. Live-verified upsert idempotency against prod with cleanup: 3 upserts → 1 row, `updated_at` advanced, table back to 0 rows.
Next: Session 4 (photo gallery link from config `PhotoGalleryUrl` — **validate the scheme is http(s) before rendering**, the value is admin-editable so it's a stored-XSS vector via `javascript:`). Session 5 = My Results + NYRR link form.

### 07-29 UTC — P1L Session 2: member race calendar (NYRR events) shipped
Changed: replaced the `/portal/events` placeholder with a real calendar. NEW `app/api/events/calendar/route.ts` (`requireActiveMember`) + `lib/db/events.ts` (RSVP-annotated event read; counts via grouped subquery so a busy event can't fan out its row; `DATE_FORMAT` so no mysql2 Date reaches the client) + `lib/events-range.ts` (civil-date math, default −1/+3 months, `MAX_RANGE_DAYS=400` clamp, 400 on from>to) + `EventsCalendarClient.tsx` w/ `_components/{MonthGrid,EventList,eventMeta}` (grid ≥lg, list below, EN/中文). Added the `/api/events` → `active` rule to `lib/access.ts` (without it the route defaulted to `public`) and a **Race Calendar** sidebar entry — `/portal/events` previously had no nav entry at all.
Status: V037 verified applied in prod (columns, `uq_rsvp_event_member`, both FKs, config row). jest **626 pass / 0 fail** across 58 suites (+64 new: 22 range math incl. DST + Jan-31 month clamp, 15 route contract incl. member-scoping and the 500 path, 15 meta helpers, 12 RTL render incl. the date-shift guard), tsc + eslint clean, `npm run build` green. Ran the calendar SQL against prod (9 rows in the default window) and smoke-tested the live dev server: `/api/events/calendar` 307s to `/login` anonymously exactly like `/api/photos`, public `/api/hof/series` still 200s. Every upcoming event has NULL distance AND distance_km, so the field hides entirely.
Next: Session 3 (RSVP + roster). **New landmine recorded in CLAUDE.md — a *value* import from `lib/db/*` into a client component pulls mysql2 into the browser bundle and fails the build (`Can't resolve 'net'`); `import type` is safe. Only `npm run build` catches it, not tsc/jest.** Also: this worktree needs `npm ci` before any webapp work.

### 07-29 UTC — P1L plan + Session 1: V037 migration written, discovery moved to weekly
Changed: wrote the 5-session P1L dev plan into CLAUDE.md (migration+data check → calendar → RSVP/roster → gallery link → My Results); product calls: shared RSVP roster w/ per-member opt-out, NYRR events only, gallery URL in config table. Session 1 shipped `db/MIGRATION_V037.sql` — `nyrr_event_rsvps` (UNIQUE(nyrr_event_id, MemberID) → RSVP change is an UPSERT; both FKs CASCADE) + `members.ShowRsvpPublicly` DEFAULT 1 + config `PhotoGalleryUrl`; all INFORMATION_SCHEMA-guarded, config seeded with INSERT IGNORE so a re-run can't stomp an admin edit. Confirmed V036 is latest → V037 correct, and all three objects absent. Also moved `nyrr_scheduler.DISCOVERY_CRON` monthly→weekly (`0 6 * * 1`). Survey found no new columns needed for result linking: `members.NYRRRunnerName`/`YearBorn` exist and `match_method` enum already has `'manual'`.
Status: **V037 NOT APPLIED** — `gh workflow run run-db-migrations.yml --ref main -f migration_version=V037` after merge; Session 2 is blocked on it. Data reality check: only **8** future-dated events (08-05 → 11-01), Sept+Oct 2026 **empty** — not a bug, NYRR publishes ~8 weeks out (07-06 discovery found nothing past 08-26), hence the weekly cron + a calendar defaulting to −1/+3 months. DB is really MySQL **8.4.8-azure**, not 5.7 (CLAUDE.md's rule is stale but safely conservative).
Next: apply V037 + verify, then Session 2 (calendar). Landmines recorded in the plan: `lib/access.ts` has no `/api/events` rule so new routes silently default to `public`; `event_date` renders a day early unless it goes through `lib/date.ts`; `distance` is NULL on most upcoming rows.
