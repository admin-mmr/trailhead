### 09-23 UTC — Static HTML playground at /playground
Changed: NEW `web-apps/mmr-webapp/public/playground/` for standalone HTML pages served as-is (first page: `capstone-project-atlas.html`) + an `index.html` list; one `rewrites()` entry in `next.config.mjs` maps bare `/playground` to it. Public by default (unmatched prefix in `lib/access.ts`), all pages `noindex`.
Status: verified on local dev — `/playground`, `/playground/index.html` and the page all 200 and render.
Next: add a page = drop the `.html` in the folder + one `<li>` in `index.html`. `www.mmrunners.org` does not serve this app; use `orange-tree-0d70d110f.4.azurestaticapps.net`.

### 09-15 UTC — Lantern on /join + /donate; multi-language pulled back to one language per toggle
Changed: **Restyle.** Both payment flows moved onto the Lantern palette — 191 mechanical swaps (legacy `#0A2342`, which was never in the brand palette, splits by role: text→`lantern-ink`, interactive→`brand-crimson`) plus every remaining arbitrary hex tokenised. Page shells got the blush gradient + warm card; both steppers use GOLD for completed steps (green was the only cool colour left on a warm page). Diff verified to be styling-only — no flow logic touched. **Three bugs the restyle itself introduced and one it inherited:** primary buttons went crimson but kept a navy `hover:bg-[#0d2d55]`; `hover:border-gray-300`→`hover:border-lantern-line` made hover identical to the resting border (a no-op); selected plan/payment states used `bg-red-50`, **the same background as the field-error styling**, so a chosen payment method read as a validation failure (now gold tint). **Multi-language.** User feedback: "English version include Chinese and vice versa" — the bilingual-inline idea from CLAUDE.md's newbeerunning research was over-applied, from the nav into headings, card subtitles and tile labels. The language toggle is now authoritative EVERYWHERE. Also fixed two strings that were **Chinese-only regardless of language**: the home CTA heading (`加入岚山跑团`) and the hero motto badge.
Status: tsc + eslint clean, jest **1020 pass / 0 fail** (83 suites). Verified both pages and the home page in **both** languages in the browser — English mode now contains no Chinese and vice versa.
Next: ⚠️ **The hero bilingual lockup is gone** — 岚山跑团 + Misty Mountain Runners together was the signature of Option C ("our identity leads"). It now follows the toggle. This was a judgement call, NOT a user decision (the question was asked and not answered); `HomeHero.tsx` carries a comment saying exactly how to restore it if the board wants the lockup back. `/faq` + `/hall-of-fame` are still un-styled. `PLANS` in `join/_components/shared.tsx` still hardcodes 30/50/20 while the home CTA reads the config table — pre-existing, worth reconciling (P1k item 2).

### 09-15 02:49 UTC — Website design vote: C · Lantern picked and built (home page)
Changed: Closed `website-design-2026` (was still `status='open'`). **The result was NOT decisive — 10 ballots, C 12 Borda vs J 11, a one-ballot margin, first-choices flat at C=2/H=2/rest 1.** Per the user's call, built **C · Lantern as the base with J's ideas folded in**: new Lantern palette + Sora/Noto-900 in `tailwind.config.ts`/`globals.css`; `app/(public)/page.tsx` is now a **server** component (was `'use client'`) feeding `_components/Home{Hero,Stats,Events,Benefits,Explore,JoinCta}`. Borrowed from J: the **dated-events rail** (real `nyrr_events`, the research finding our site was missing) and **photographs-as-navigation** tiles. NEW `lib/db/events-public.ts` — public, member-free reads, deliberately separate from the member-scoped `lib/db/events.ts` so no public caller can surface an RSVP. Cross-cutting win shipped regardless of design: **bilingual inline nav** (`components/layout/NavLabel.tsx` + `pair()` in translations) — "首页 Home" always shows both, reader's language leads, instead of hiding one behind the toggle.
Status: tsc + eslint clean, jest **1020 pass / 0 fail** across 83 suites (+11 for the event feed), `npm run build` exits 0. Verified in the browser at desktop + 375px in **both** languages: no horizontal overflow, clean console on a fresh tab. Two bugs found and fixed only by looking — stat strip needed `relative z-10` or the hero painted over the numbers, and the 4-col Explore grid left row 2 half-empty with the label overlapping the placeholder (now an even 2×2). Prices in the CTA come from the **config table**, not hardcoded, so the page can't quote a number Checkout won't charge. `Navbar.tsx` hit 311 lines so `NavLabel` was extracted (279 + 37).
Next: **NOT COMMITTED** (repo rule: no commit without an explicit request). Two data realities to know — only **1** future NYRR race exists (Nov 1 marathon; NYRR publishes ~8wk out, discovery is running fine), so the rail backfills with recent races and says so; and **4 of the Explore tiles want real member photographs** — two render an honest placeholder until the board sends the 6–7 shots already being chased. Other pages (`/join`, `/donate`, `/faq`, `/hall-of-fame`) are still the old style — the Lantern treatment stops at the home page for now.
