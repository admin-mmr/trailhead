### 07-30 UTC — Membership: rolling expiration, weekly renewal reminders, family notifications
Changed: **db/MIGRATION_V038.sql (WRITTEN, NOT APPLIED)** — `fn_next_expiration(base,anchor,years)` (DETERMINISTIC NO SQL, the only definition of `MAX(current+1yr, anchor+1yr)`), rewritten `trg_payments_sync_membership_only` (family MAX as the base so no relative loses coverage; **lifetime members no longer stomped** — they were previously overwritten to the club year end), rewritten `sp_reconcile_member_payments` (**was force-setting Expiration=MembershipYearEnd and would have reverted every rolling date**; now targets payment_date+1yr and is `GREATEST`-guarded so it is idempotent and can never shorten a membership), `notification_log` (UNIQUE `dedupe_key` = the idempotency guard), config `RenewalRemindersEnabled`/`RenewalReminderMaxPerRun`. Webapp: `lib/membership/{expiration,renewal-stages}.ts` (5 contiguous day-bands T60/T30/T7/LAPSED_14/FINAL_45, min width 15d so a weekly job can't step over a member), `lib/db/{renewals,notifications}.ts`, `lib/notifications/{send-tracked,renewal-reminders,family}.ts` (claim→send→settle; a failed send NULLs the dedupe key so the next run retries), `POST /api/jobs/renewal-reminders` + `.github/workflows/renewal-reminders.yml` (Tue 15:00 UTC), `POST /api/notifications/family-updated`, `lib/email/registry.ts` + `GET /api/admin/email-preview`, family fan-out in `fulfillment-email.ts`, `mmr-admin/webapp_notify.py` wired into add-member + upgrade-and-add.
Status: jest **908 pass / 0 fail** (+109), pytest family/members suites 218 pass + 10 new webapp_notify, tsc + eslint clean, `npm run build` green, coverage floors held. Every new file under the size limits.
Next: **apply V038** (`gh workflow run run-db-migrations.yml --ref main -f migration_version=V038`) then verify against the DB — nothing renews correctly until it lands. Then set `JOB_SECRET` in BOTH the GitHub Actions secrets and the SWA app settings (paste the real value — placeholder pastes have broken this repo twice) and run the workflow with dryRun. ⚠️ All 408 active members share Expiration 2027-03-31, so the first T60 band opens for all of them at once — the per-run cap (150) drains it over ~3 weeks.

### 07-29 UTC — P1L Sessions 4+5: photo gallery link + My Results with self-service NYRR linking
Changed: **S4** — `lib/safe-url.ts` (`isSafeHttpUrl`/`safeHttpUrlOr`: http(s) only; rejects `javascript:`/`data:`/protocol-relative AND embedded whitespace/control chars, since browsers strip those before dispatch so `java\nscript:` is live in an href but passes a naive startsWith check) + `lib/db/gallery.ts` reading config `PhotoGalleryUrl`; rendered as `<a target=_blank rel=noopener noreferrer>` in the sidebar + a dashboard card (plus a Race Calendar card), read server-side in the layout/page inside try/catch so a DB hiccup hides the link rather than breaking the portal shell. **S5** — `lib/db/nyrr-results.ts` + `GET /api/members/me/nyrr-results`, `POST …/nyrr-link` (saves name+year, returns **candidates only**), `POST …/nyrr-link/confirm`. Access rule `/api/members/me/nyrr` → `active` placed ABOVE the looser `/api/members/me` → `member`. The orphaned `NyrrClient.tsx` is now **reused** (working recharts dashboard; adapter maps MemberResult→NyrrResult); new `NyrrLinkForm` does collect→confirm; sidebar `/portal/nyrr` un-disabled. Confirm treats `runnerIds` as untrusted: intersected with a fresh candidate set (forged id ⇒ **no UPDATE issued at all**), criteria re-read from the DB not the body, UPDATE re-asserts the ownership guard, writes `match_method='manual'`/`matched_by='member:<id>'` and refreshes `mmr_matched_count`.
Status: jest **799 pass / 0 fail** across 66 suites (+106), tsc + eslint clean, build green, coverage 77/88/78. Two prod-verified perf/correctness facts: `runner_name = ?` uses `idx_runner_name` (6 rows examined) whereas `LOWER(TRIM(runner_name))` scans **1,634,888** rows / ~8s — so name comparisons use plain `=` (columns are `utf8mb4_unicode_ci`, already case-insensitive) and the *input* is trimmed instead; and the candidate query returned 3 correct rows for a real runner while a birth year off by 25 returned 0.
Next: nothing outstanding in P1L — all 5 sessions shipped. **NEW P1m logged in CLAUDE.md (pre-existing, not mine):** admin `api_events_match.py` Tier-2 validates age against `YEAR(CURDATE())` instead of the event year, and **8 of 360 linked members have impossible age spreads** (A0022: ages 8–65 across 2018–2026) — members can now SEE those wrong rows at `/portal/nyrr`. Also: only 50 members have `NYRRRunnerName` and none of them also have `YearBorn`.

### 07-29 UTC — P1L Session 3: event RSVP + shared roster with privacy opt-out
Changed: `POST/DELETE /api/events/[id]/rsvp` (upsert via the V037 unique key — values passed twice as params since `VALUES()` in the UPDATE clause is deprecated in MySQL 8.0.20+; 404 on unknown event checked *before* the write so the FK can't become a 500; **409 on a past race**, race day itself still open) + `GET /api/events/[id]/roster`. **The `ShowRsvpPublicly` filter lives in `getEventRoster()`, not the route** — opted-out members are counted via `hiddenCount` but neither their name nor their note enters the payload, and no future caller can leak one by forgetting to filter. `not_going` counted, never listed; nameless members fall back to MemberID, never email. Opt-out plumbed `Member.showRsvpPublicly` → `rowToMember` (absent/NULL ⇒ **true**, matching the DB default) → `updateMemberProfile` (writes 1/0) → `PATCH /api/members/me` (`z.boolean()`, so `"false"`/`0` are rejected not coerced) → checkbox on `/portal/profile`. New `_components/{RsvpControls,RosterPanel,EventCard}`; re-tapping the selected intent clears it. Also moved month nav out of `MonthGrid` into the container — the grid is hidden below `lg`, so nav inside it was unreachable on mobile — and the card list now renders in month view too, since a desktop member on the grid otherwise had no way to RSVP.
Status: jest **693 pass / 0 fail** across 62 suites (+55: 26 rsvp route, 14 roster incl. asserting the opted-out name/id/note appear nowhere in the serialized body, 15 RSVP/roster UI incl. the 307-returns-HTML case, plus privacy-column mapping and PATCH boolean-strictness). tsc + eslint clean, `npm run build` green, coverage 75%/87%/77% vs floors 30/75/43. Live-verified upsert idempotency against prod with cleanup: 3 upserts → 1 row, `updated_at` advanced, table back to 0 rows.
Next: Session 4 (photo gallery link from config `PhotoGalleryUrl` — **validate the scheme is http(s) before rendering**, the value is admin-editable so it's a stored-XSS vector via `javascript:`). Session 5 = My Results + NYRR link form.

