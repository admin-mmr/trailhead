### 07-31 UTC — Observability: App Insights wired up (#3) + schema snapshot regenerated (#4)
Changed: **Telemetry.** `lib/telemetry.ts` posts envelopes straight to the App Insights ingestion REST API (`<IngestionEndpoint>v2/track`) with plain `fetch` — no SDK. The `applicationinsights` v3 SDK was tried first and **rejected**: it pulls `@azure/monitor-opentelemetry` → `@opentelemetry/sdk-node` → `@grpc/grpc-js`, which needs `net`/`tls`/`fs`/`stream`/`zlib`, and Next compiles `instrumentation.ts` for the **edge** runtime too, so `npm run build` failed with "Can't resolve 'net'". Externalising that whole tree is brittle and the SDK is a heavy import for an app whose SWA cold start is already ~19s. Trade-off accepted and documented in the file: **no free `requests`/`dependencies` telemetry (no automatic MySQL timings)** — `trackDependency()` is opt-in for paths where latency has bitten. Wired `trackException()` into `withApiHandler`'s catch (the one place unhandled API errors surface) with **pathname only, never the full URL**, since query strings can carry member identifiers and telemetry is retained far longer than a request log. **Snapshot.** `db/schema_snapshot.sql` regenerated from `db/queries/schema_snapshot_query.sql` (no mysql client here, so a Node script reproduced the CLI's TSV formatting: `NULL` literal, `\n`/`\t` escaping, one header row per result set). It was stale by **three** migrations, not one — the diff adds `nyrr_event_rsvps` (V037), `notification_log` + `fn_next_expiration` (V038), the four poll tables (V039), and replaces `sp_reconcile_member_payments` + `trg_payments_sync_membership_only`; 617 → 712 lines, nothing structural lost.
Status: jest **946 pass / 0 fail** across 77 suites (+10 telemetry: envelope shape, region endpoint, no-op when unconfigured, never-rejects, duration formatting), tsc + eslint clean, `npm run build` exits 0, both files well under the 300-line limit. **Verified live against `mmr-appinsights`**, not just unit-tested: exception, trace and dependency envelopes all returned `itemsAccepted:1, errors:[]` and then appeared in `exceptions`/`traces`/`dependencies` with `cloud_RoleName = mmr-webapp`. Two traps found by doing it live — **ingestion latency is ~2-4 minutes** (which twice made in-flight items look dropped, and I twice drew a wrong conclusion from it), and `AbortSignal.timeout` is **absent under jsdom**, so referencing it unguarded threw before `fetch` was called and silently dropped every item (now feature-detected).
Next: nothing outstanding on these two. Still queued for after the weekend, per the user: **`STRIPE_WEBHOOK_SECRET` is 7 chars in SWA settings** (real ≈38+) so Stripe webhook signature verification is broken in prod right now; **`JOB_SECRET` missing** so renewal reminders send nothing; share the poll and chase the board for a decision plus **six or seven member photographs**. Consider `trackDependency()` around the `members` lookup in `/auth/complete` if login latency is ever questioned again.

### 07-30 UTC — Google login: spinner never resolved (client-side, not OAuth config)
Changed: reported symptom was "Continue with Google spins forever, no timeout". Ruled out the server end against prod — provider registered, Google *accepts* the redirect URI (followed the authorize URL to a real sign-in page), the JSON `X-Auth-Return-Redirect` POST returns a valid `{url}`, `/auth/complete` answers in ~0.15s. The hang is the last step of next-auth's `signIn()`: `window.location.href = <google url>`, which never commits when `accounts.google.com` is unreachable (mainland China, or a WeChat/QQ webview refusing the hop) — and `handleOAuth` had **no try/catch and no timeout**, unlike `handleCredentials`. Added a 6s watchdog offering the email+password fallback, a catch for a rejecting `signIn`, and NEW `lib/is-webview.ts` replacing the inline UA check that **omitted WeChat** — Android WeChat reads as ordinary Chrome (`MicroMessenger` + no `; wv)`), so the existing "open in a browser" banner never fired for the club's main sharing channel.
Also fixed: **`/auth/complete` no longer creates members.** An unrecognised OAuth email used to mint a fresh `pending` row and redirect to `/join`, which forked an existing member onto a second MemberID whenever their Google address differed from the one on file — and 130+ active members are on yahoo/hotmail/aol/msn, so mismatch is the norm. It now redirects to `/login?error=oauth_no_member` (**no email in the query string** — that would land in browser history and edge logs) with bilingual copy naming the likely cause and the "Forgot password" route. The `/join` branch is gone (`DEFAULT_DESTINATION`); signing in is not signing up, and `/join` + the admin panel still collect the phone/district/type/payment a social profile can't supply. Verified prod has **0 rows** matching the old auto-create shape (pending + no phone + no WeChat + no payment), so there is nothing to clean up.
Also split: `app/login/page.tsx` **370 → 94 lines** → `login/_components/` (`useLoginFlow.ts` 120, `CredentialsForm` 99, `SocialSignIn` 81 incl. WebViewWarning + StallNotice, `messages.ts` 55, `shared.tsx` 38, `GoodbyeBanner` 33) — same `useXFlow` + views shape as `join/`/`donate/`. The 4 stall tests passed **unchanged** across the refactor, which is the evidence it preserved behaviour. Extracting the error copy exposed a **real pre-existing i18n bug**: the `?error=` message was seeded in a `useState` initializer, which runs on the first render while `lang` is still the 'en' default (LanguageProvider hydrates from localStorage in an effect AFTER mount) — so a 中文 member got Chinese buttons and an English error, permanently. Now derived each render (`error ?? loginErrorMessage(...)`, `null` = not yet overridden, so handlers can still set/clear it). Verified in a browser: `mmr-lang=zh` renders the whole card in Chinese, error included.
Also fixed: **`?from=` is now honoured, and validated.** The credentials path already sent `/auth/complete?from=<returnTo>` and the route ignored it, so every member was dumped on `/portal` instead of the page they wanted; the OAuth path dropped `from` entirely. Both now carry it and `resolveDestination()` gates it through `isSafeSitePath()` — `from` is attacker-controllable and the hop happens right after a real login, so an unchecked value would be a convincing open redirect. Off-site forms (absolute URL, `//evil.com`, `/\evil.com`, `javascript:`, bare relative) and loop targets (`/login`, `/auth/`, `/join`) all fall back to `/portal`.
Status: jest **999 pass / 0 fail** across 81 suites (+63: 25 real-UA cases incl. both WeChat platforms, 4 RTL stall/dismiss/reject, 9 first-ever `/auth/complete` contract tests pinning "never creates a member" + no-PII-in-redirect + expired-active downgrade, 10 error-copy, 4 language regression driving the REAL LanguageProvider, 11 redirect-destination incl. 5 open-redirect forms). tsc + eslint clean, build exits 0, coverage 81.5/88.4/78.5. No DB or migration changes.
Next: **check the OAuth consent screen** — 654 of 657 members have never signed in by any method, and the only 3 `google_sub` rows are insiders, which is also what "Testing" publishing status looks like (console → `mmr-data-pipeline` → OAuth consent screen → Publish app). Still open: `lib/email/templates/payments.ts` is 317 lines vs the 300 limit. ⚠️ Never run `npm run build` while `mmr-web` is up — both write `.next` and the dev server then 500s with `Cannot find module './NNNN.js'`; restart it, the code is fine.

### 07-30 UTC — Poll: full-length design previews (V040)
Changed: poll cards showed only the hero crop, so voters could not judge how a design behaves further down the page. Each option now links to its **real scrollable mockup** — the ten HTML files are served statically from `public/mockups/`, with their `img/` refs rewritten to `/images/` so they reuse assets the app already ships rather than duplicating ~500KB. `MIGRATION_V040` fills `poll_options.detail_path` (`current` → `/`). Card restructured: the link is a **sibling** of the select `<button>`, not a child (an `<a>` inside a `<button>` is invalid HTML and clicks resolve ambiguously), and the hero now carries an explicit "Top of the page only" caption so nobody assumes the crop is the whole design. NEW `isSafeSitePath()` + `safeLinkOrNull()` in `lib/safe-url.ts`: `detail_path` is a DB-sourced href and `isSafeHttpUrl` rejects relative paths, so without this the choice was "no validation" or "no relative links".
Status: jest **936 pass / 0 fail** across 76 suites (+15: 8 path-guard incl. protocol-relative `//evil.com` and `/\evil.com`, hyphenated-path regression, and `java\nscript:`; 7 RTL incl. asserting the link is not inside a button and is hidden for a hostile `detail_path`). tsc + eslint clean, build exits 0. Verified all ten mockups serve `200 text/html` locally with `/images/mmr-*` resolving, and rendered option J in a browser.
Next: merge, then apply V040 — **in that order**. Applying it first would publish `/mockups/*.html` links against a deploy that does not have the files yet, i.e. live 404s from the poll page.
