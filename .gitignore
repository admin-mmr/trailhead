# Google App Engine generated folder
appengine-generated/
# Azure Functions generated folder

# Environment variables (all directories, all variations)
.env
.env.local
.env.*.local
**/.env
**/.env.local
**/.env.*.local
**/.env.local.gpg

node_modules/
test-photos/
# Race-result data dumps. This is a blanket directory rule, so it also swallowed
# App Router source at app/**/results/ — a /poll/[slug]/results page vanished
# from a commit with no warning. Keep webapp source re-included.
results/
!web-apps/mmr-webapp/app/**/results/
!web-apps/mmr-webapp/app/**/results/**
azurite-data/
azurite-debug.log
mmr-admin/nyrr_api.py
mmr-admin/sync_engine.py
mmr-admin/sync_config.py
dist/
*.js.map

# Python
.venv/
__pycache__/
**/__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Local photo albums (CV experiment inputs — not for repo)
album_*/

# CV experiment outputs
output*.json
best_photos.json
rejected_photos.json
debug_out/
bib_results/
profiles/

# ML model weights (large binaries — download separately)
*.pt
*.pth
*.onnx

# macOS
.DS_Store
.AppleDouble

# Office documents (commit markdown versions instead)
*.docx
*.xlsx
*.pptx

# Temp Office lock files
~$*.docx
~$*.xlsx
~$*.pptx

# CI-generated version file (written at deploy time)
mmr-admin/VERSION

# SQLite databases (runtime artifacts)
*.db
*.db-shm
*.db-wal

# Orphaned sync scripts (functionality moved to basecamp/run-sync.sh and Admin Portal)
/sync_gmail.sh
/sync_members.sh
/sync_payments.sh
/sync_webapp_events.sh

# Orphaned sheets sync duplicates (use mmr-admin/api_sheets_sync.py instead)
/api_sheets_sync.py
/api_sheets_sync_batched.py

# Legacy schema/migration folders (db/ is the source of truth)
/basecamp/schemas/
/basecamp/migrations/

# Local git worktrees
.claude/worktrees/

# Coverage artifacts (CI and the documented `pytest --cov` run both create these)
.coverage
.coverage.*
htmlcov/

