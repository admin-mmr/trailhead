{
  "name": "mmr-admin",
  "version": "0.0.0",
  "private": true,
  "description": "Lint infrastructure for mmr-admin static browser components (no build step)",
  "scripts": {
    "lint": "eslint static/"
  },
  "devDependencies": {
    "eslint": "^9.31.0",
    "globals": "^16.3.0"
  }
}
